# Training and evaluation. Install torch first if you need a specific CUDA build:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128

torch>=2.4
torchvision>=0.19
ultralytics>=8.4
aim>=3.29,<4
albumentations>=2.0       # augmentation, including the bbox-aware transforms
# Server/cloud build. Do not install this together with opencv-python or either
# opencv-contrib wheel: all four distributions share the same cv2 namespace.
opencv-python-headless>=4.10,<5
numpy>=1.26
PyYAML>=6.0

# Only needed to download Open Images. prepare_data.py reads the CSVs itself,
# so this is not required once the files are on disk.
fiftyone>=1.0
